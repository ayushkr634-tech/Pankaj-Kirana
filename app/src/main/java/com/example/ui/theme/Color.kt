package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Light Theme Palette (High Density Theme)
val HighDensityBg = Color(0xFFF7FBF4)       // Primary Light Organic Green background
val HighDensityText = Color(0xFF191C19)     // Rich charcoal-green for high-density reading
val HighDensityPrimary = Color(0xFF1B6C31)  // Rich forest green for accents, branding, buttons
val HighDensitySecondary = Color(0xFFD0EBCB)// Promo/Banner green
val HighDensityTertiary = Color(0xFFD6E8D8) // Badge and navigation pill background
val HighDensitySurface = Color(0xFFFFFFFF)  // White card/surface background
val HighDensityOutline = Color(0xFFE1E5E1)  // Soft border line for high density grids
val HighDensitySubtext = Color(0xFF414941)  // Muted body/label text

// Dark Theme Palette (Sleek High Density Forest Night)
val DarkForestBg = Color(0xFF111D13)      // Deep dark forest green night background
val DarkCardBg = Color(0xFF1B241C)        // High-density dark cards
val PastelOrganicGreen = Color(0xFF8FD8A0)// Highly readable light green
val PastelWarmAmber = Color(0xFFE5B075)   // Muted gold/amber for high density dark highlights
val LightGrey = Color(0xFFE2E7E2)         // High contrast light text
val DarkOutline = Color(0xFF2D3C2F)       // High-density dark boundaries
